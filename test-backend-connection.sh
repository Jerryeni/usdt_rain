#!/bin/bash

# Test backend connection from different methods

echo "╔══════════════════════════════════════════════════════════════╗"
echo "║                                                              ║"
echo "║   🧪 TEST BACKEND CONNECTION                                 ║"
echo "║                                                              ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo ""

BACKEND_URL="http://147.93.110.96:3001"

echo "Testing backend at: $BACKEND_URL"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Test 1: Health endpoint"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

curl -v "$BACKEND_URL/api/v1/health" 2>&1 | head -30

echo ""
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Test 2: Root endpoint"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

curl -v "$BACKEND_URL/" 2>&1 | head -30

echo ""
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Test 3: Check if port 3001 is accessible"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

nc -zv 147.93.110.96 3001 2>&1 || telnet 147.93.110.96 3001 2>&1 | head -5

echo ""
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "RESULTS:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

if curl -s -m 5 "$BACKEND_URL/api/v1/health" > /dev/null 2>&1; then
  echo "✅ Backend is accessible from your machine!"
  echo ""
  echo "Response:"
  curl -s "$BACKEND_URL/api/v1/health" | jq . 2>/dev/null || curl -s "$BACKEND_URL/api/v1/health"
else
  echo "❌ Backend is NOT accessible from your machine"
  echo ""
  echo "Possible reasons:"
  echo "1. Port 3001 is blocked by VPS firewall"
  echo "2. Backend is not listening on 0.0.0.0 (only localhost)"
  echo "3. Network firewall blocking the connection"
  echo ""
  echo "Solution: Use a domain with web server proxy instead of direct port access"
fi

echo ""
